<article>
  <h2>Message Details</h2>
  <p><%= message.user %></p>
  <p><%= message.text %></p>
  <p>
    <%= formatTime(message.added)[0].toUpperCase() +
    formatTime(message.added).slice(1) %>
  </p>
</article>
